channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base
  - r-phangorn
  - r-ggplot2
  - r-remotes
  - bioconductor-ggtree
  - bioconductor-tanggle
  # fastnntr is CRAN-only and is installed by install_fastnntr before inference.
