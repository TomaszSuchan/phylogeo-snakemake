channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base
  - r-adegenet
  - r-vcfr
  - r-poppr
  - r-geosphere
  - r-ggplot2
  # conStruct is installed from CRAN in workflow/scripts/construct_analysis.R if not available.
