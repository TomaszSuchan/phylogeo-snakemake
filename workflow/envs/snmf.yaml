channels:
  - conda-forge
  - bioconda
dependencies:
  # sNMF is provided by the LEA Bioconductor package (Frichot & Francois 2015);
  # unlike tess3r it is on bioconda, so no source-install rule is required.
  - bioconductor-lea=3.22.0
  - r-base
  - r-vcfr
  - r-ggplot2
