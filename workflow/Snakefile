# Canonical Set1 palette — the Python-level fallback for structure_colors.
# Must match the list in config.yaml mapmixture.structure_colors so that
# explicit config and the rule default always agree.
_DEFAULT_STRUCTURE_COLORS = [
    "#E41A1C", "#377EB8", "#4DAF4A", "#984EA3", "#FF7F00",
    "#FFFF33", "#A65628", "#F781BF", "#999999", "#FFFFFF", "#000000",
]


def _spca_n_axes_from_params(params):
    """Number of retained sPCA axes (global + local) for plot targets."""
    sp = params.get("spca", {})
    nfposi = int(sp.get("nfposi", 2))
    nfnega = int(sp.get("nfnega", 2))
    return nfposi + nfnega


def _spca_nfposi_from_params(params):
    """Number of retained positive/global sPCA axes."""
    return int(params.get("spca", {}).get("nfposi", 2))


def _spca_nfnega_from_params(params):
    """Number of retained negative/local sPCA axes."""
    return int(params.get("spca", {}).get("nfnega", 2))


def _choose_k_plot_param(wildcards, key, default):
    """Width, height, or dpi for Evanno-style choose-K score plots."""
    return config["projects"][wildcards.project]["parameters"].get("choose_k_plot", {}).get(key, default)


def get_pca_combinations(pc_max):
    """
    Generate all unique combinations of PC axes up to pc_max.
    For example, if pc_max=4, generates: (1,2), (2,3), (3,4), (1,3), (2,4), (1,4)
    """
    combinations = []
    for i in range(1, pc_max + 1):
        for j in range(i + 1, pc_max + 1):
            combinations.append((i, j))
    return combinations


def _map_basemap_name(wildcards):
    """Normalized basemap selector from project config."""
    basemap = (
        config["projects"][wildcards.project]
        .get("parameters", {})
        .get("map_background", {})
        .get("basemap", "NULL")
    )
    return str(basemap).strip().lower()


def _use_elevation_bg(wildcards):
    """Whether elevation DEM background should be used."""
    return _map_basemap_name(wildcards) in {"elevation", "dem", "elevatr", "terrain"}


def _natural_earth_kind(wildcards):
    """Natural Earth raster kind expected by fetch_naturalearth_hr_global.R."""
    basemap = _map_basemap_name(wildcards)
    if basemap in {"gray_earth", "grey_earth", "gray", "grey"}:
        return "gray_earth"
    # Default to NE1 for null/unknown values and non-elevation map backgrounds.
    return "ne1"


def _map_background_inputs(wildcards):
    """Optional cached basemap inputs used by map plotting scripts."""
    base = {}
    if _use_elevation_bg(wildcards):
        base["elevation_basemap"] = f"results/{wildcards.project}/maps/elevation_basemap.tif"
    elif _map_basemap_name(wildcards) in {"ne1", "gray_earth", "grey_earth", "gray", "grey"}:
        base["naturalearth_basemap"] = f"results/{wildcards.project}/maps/naturalearth_basemap.tif"
    return base


def _structure_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/structure/{wildcards.project}.structure.K{wildcards.k}.Qmatrix.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _faststructure_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/faststructure/{wildcards.project}.faststructure.{wildcards.k}.meanQ",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _admixture_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/admixture/{wildcards.project}.biallelic_snps_thinned.{wildcards.k}.Q",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _tess3_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/tess3/{wildcards.project}.tess3.K{wildcards.k}.Qmatrix.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _snmf_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/snmf/{wildcards.project}.snmf.K{wildcards.k}.Qmatrix.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _alstructure_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/alstructure/{wildcards.project}.alstructure.K{wildcards.k}.Qmatrix.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _tess3_ggmap_inputs(wildcards):
    inputs = {
        "results_rds": f"results/{wildcards.project}/tess3/{wildcards.project}.tess3.K{wildcards.k}.results.rds",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/tess3_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _construct_map_inputs(wildcards):
    inputs = {
        "qmatrix": f"results/{wildcards.project}/construct/{wildcards.project}.construct.K{wildcards.k}.Qmatrix.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _dapc_map_inputs(wildcards):
    inputs = {
        "dapc_results": f"results/{wildcards.project}/dapc/{wildcards.project}.dapc.K{wildcards.k}.results.rds",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _population_map_inputs(wildcards):
    inputs = {
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _spca_map_inputs(wildcards):
    inputs = {
        "results_rds": f"results/{wildcards.project}/spca/{wildcards.project}.spca.results.rds",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _pixy_grouping_columns(project_params):
    """
    Columns used as pixy `{grouping}` wildcards (and in result filenames).

    Reads, in order: `pixy.group_by` (list or string), then legacy `groupings`
    / `grouping`. Defaults to Site when nothing is set.
    """
    pixy_cfg = project_params.get("pixy", {})
    gb = pixy_cfg.get("group_by")
    if isinstance(gb, list) and gb:
        return [str(x) for x in gb if x and str(x) != "none"]
    if isinstance(gb, str) and gb and gb != "none":
        return [gb]
    groupings = pixy_cfg.get("groupings")
    if isinstance(groupings, list) and groupings:
        return [str(g) for g in groupings]
    if isinstance(groupings, str) and groupings:
        return [groupings]
    grouping = pixy_cfg.get("grouping")
    if grouping:
        return [str(grouping)]
    return ["Site"]


def _pixy_enabled_stats(project_params):
    """Which pixy statistics are requested (`pi`, `fst`, `dxy`)."""
    pixy_cfg = project_params.get("pixy", {})
    stats = pixy_cfg.get("stats", "pi fst dxy")
    if isinstance(stats, list):
        return {str(s).strip().lower() for s in stats if str(s).strip()}
    if isinstance(stats, str):
        return {t.lower() for t in stats.replace(",", " ").split() if t.strip()}
    return {"pi", "fst", "dxy"}


def _pixy_metadata_table_path(project, params):
    """Metadata table for counting pixy populations (indpopdata preferred, else popdata)."""
    import os

    indpopdata = f"results/{project}/indpopdata.txt"
    if os.path.exists(indpopdata):
        return indpopdata
    popdata = params.get("popdata")
    if popdata and popdata not in ("NULL", "") and os.path.exists(popdata):
        return popdata
    return None


def _pixy_groupings_with_min_populations(project, params, min_pops=2):
    """
    Grouping columns with at least `min_pops` unique values for pairwise pixy stats.

    FST and dXY require population pairs; groupings below the threshold are omitted
    from fst/dxy targets. When metadata is unavailable at DAG build time, returns all
    configured groupings and the pixy_fst/pixy_dxy rules skip at runtime.
    """
    import csv

    groupings = _pixy_grouping_columns(params)
    metadata_path = _pixy_metadata_table_path(project, params)
    if metadata_path is None:
        return groupings

    with open(metadata_path, newline="") as handle:
        reader = csv.DictReader(handle, delimiter="\t")
        if not reader.fieldnames:
            return groupings
        rows = list(reader)

    eligible = []
    for col in groupings:
        if col not in reader.fieldnames:
            continue
        unique_vals = {row[col].strip() for row in rows if row.get(col, "").strip()}
        if len(unique_vals) >= min_pops:
            eligible.append(col)
    return eligible


def _relatedness_color_by_columns(project_params):
    """Columns used as relatedness plot `{color_by}` wildcards."""
    relatedness_cfg = project_params.get("relatedness_plot", {})
    cb = relatedness_cfg.get("color_by")
    if isinstance(cb, list) and cb:
        return [str(x) for x in cb if x and str(x).strip().lower() != "none"] or ["none"]
    if isinstance(cb, str) and cb.strip() and cb.strip().lower() != "none":
        return [cb.strip()]
    return ["none"]


def _neighbornet_color_by_columns(project_params):
    """Columns used as NeighborNet plot `{color_by}` wildcards."""
    neighbornet_cfg = project_params.get("neighbornet", {})
    cb = neighbornet_cfg.get("color_by")
    if isinstance(cb, list) and cb:
        return [str(x) for x in cb if x and str(x).strip().lower() != "none"] or ["Site"]
    if isinstance(cb, str) and cb.strip() and cb.strip().lower() != "none":
        return [cb.strip()]
    return ["Site"]


def _pixy_pi_map_inputs(wildcards):
    """
    PI maps merge summary `population` with indpopdata `Site` coordinates.
    Only Site-stratified pixy π summaries are valid; path is fixed to `.Site.`.
    """
    inputs = {
        "summary": f"results/{wildcards.project}/pixy/{wildcards.project}.Site.pixy_pi-summary.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _has_popdata(params):
    """True when a project has a valid popdata file configured."""
    popdata = params.get("popdata")
    return bool(popdata) and popdata not in ("NULL", "")


def _map_bg_params(wildcards):
    """Shared map_background params dict for all map-producing rules.

    Includes `use_elevation_bg` since it is always needed alongside the other
    map params and is derived from the same config section.
    """
    bg = config["projects"][wildcards.project]["parameters"]["map_background"]
    params = config["projects"][wildcards.project]["parameters"]
    return {
        "width": bg.get("width", 10),
        "height": bg.get("height", 8),
        "dpi": bg.get("dpi", 300),
        "boundary": params.get("map_boundary", "NULL"),
        "crs": bg.get("crs", 4326),
        "basemap": bg.get("basemap", "NULL"),
        "land_colour": bg.get("land_colour", "#d9d9d9"),
        "sea_colour": bg.get("sea_colour", "#deebf7"),
        "expand": bg.get("expand", False),
        "arrow": bg.get("arrow", True),
        "arrow_size": bg.get("arrow_size", 1),
        "arrow_position": bg.get("arrow_position", "tl"),
        "scalebar": bg.get("scalebar", True),
        "scalebar_size": bg.get("scalebar_size", 1),
        "scalebar_position": bg.get("scalebar_position", "tl"),
        "plot_title": bg.get("plot_title", ""),
        "axis_title_size": bg.get("axis_title_size", 10),
        "axis_text_size": bg.get("axis_text_size", 8),
        "basemap_border": bg.get("basemap_border", True),
        "basemap_border_col": bg.get("basemap_border_col", "black"),
        "basemap_border_lwd": bg.get("basemap_border_lwd", 0.1),
        "use_elevation_bg": _use_elevation_bg(wildcards),
    }


def _mapmixture_pie_params(wildcards):
    """mapmixture pie chart + structure_colors params for ancestry map rules."""
    mm = config["projects"][wildcards.project]["parameters"]["mapmixture"]
    return {
        "pie_size": mm.get("pie_size", 1),
        "pie_border": mm.get("pie_border", 0.2),
        "pie_border_col": mm.get("pie_border_col", "black"),
        "pie_opacity": mm.get("pie_opacity", 1),
        "legend": mm.get("legend", False),
        "structure_colors": mm.get("structure_colors", _DEFAULT_STRUCTURE_COLORS),
    }


def _mapmixture_map_params(wildcards):
    """Combined map_background + mapmixture pie params for ancestry map rules."""
    return {**_map_bg_params(wildcards), **_mapmixture_pie_params(wildcards)}


def _barplot_params(wildcards):
    """Shared params for all ancestry barplot rules (map_background size + mapmixture barplot keys)."""
    bg = config["projects"][wildcards.project]["parameters"]["map_background"]
    mm = config["projects"][wildcards.project]["parameters"]["mapmixture"]
    return {
        "width": bg.get("width", 10),
        "height": bg.get("height", 6),
        "dpi": bg.get("dpi", 300),
        "site_dividers": mm.get("site_dividers", True),
        "divider_width": mm.get("divider_width", 0.4),
        "site_order": mm.get("site_order", "NULL"),
        "population_sort_by": mm.get("population_sort_by", "NULL"),
        "flip_axis": mm.get("flip_axis", False),
        "site_labels_angle": mm.get("site_labels_angle", 90),
        "population_labels": mm.get("population_labels", ["Site"]),
        "structure_colors": mm.get("structure_colors", _DEFAULT_STRUCTURE_COLORS),
    }


def _mapmixture_map_rule_params(wildcards, analysis_subdir, filename_stem):
    """Mapmixture map rule params including output_prefix."""
    return {
        **_mapmixture_map_params(wildcards),
        "output_prefix": (
            f"results/{wildcards.project}/{analysis_subdir}/"
            f"{wildcards.project}.{filename_stem}.K{wildcards.k}"
        ),
    }


def _barplot_rule_params(wildcards, analysis_subdir, filename_stem):
    """Shared ancestry barplot rule params including output_prefix."""
    return {
        **_barplot_params(wildcards),
        "output_prefix": (
            f"results/{wildcards.project}/{analysis_subdir}/"
            f"{wildcards.project}.{filename_stem}.K{wildcards.k}"
        ),
    }


PLOT_K_WILDCARD_CONSTRAINT = r"(?:[2-9]|[1-9]\d+)"


def _plot_k_values(k_values):
    """K values for ancestry visualisation outputs (maps, barplots; K >= 2 only)."""
    return [k for k in k_values if int(k) >= 2]


def _facet_k_values(wildcards):
    """K values used for multi-K map facet panels (K >= 2)."""
    k_values = config["projects"][wildcards.project]["parameters"]["k_values"]
    return _plot_k_values(k_values)


def _map_facet_inputs(wildcards, method):
    """Per-K map panel RDS inputs for facet rules (K >= 2)."""
    project = wildcards.project
    return {
        f"map_k{k}": (
            f"results/{project}/{method}/plots/"
            f"{project}.{method}.K{k}.map.rds"
        )
        for k in _facet_k_values(wildcards)
    }


def _structure_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "structure")


def _faststructure_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "faststructure")


def _admixture_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "admixture")


def _dapc_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "dapc")


def _snmf_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "snmf")


def _tess3_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "tess3")


def _alstructure_map_facet_inputs(wildcards):
    return _map_facet_inputs(wildcards, "alstructure")


def _map_facet_targets(project, method):
    """PDF + RDS targets for a multi-K ancestry map facet panel."""
    return [
        f"results/{project}/{method}/plots/{project}.{method}.map-facet.pdf",
        f"results/{project}/{method}/plots/{project}.{method}.map-facet.rds",
    ]


def _pixy_pi_map_params(wildcards):
    """Params for plot_pixy_pi_map (map_background + pixy map knobs)."""
    pixy = config["projects"][wildcards.project]["parameters"]["pixy"]
    return {
        **_map_bg_params(wildcards),
        "stat_type": "pi",
        "point_size": pixy.get("point_size", 3),
        "map_outline": pixy.get("map_outline", True),
    }


def _froh_map_inputs(wildcards):
    """Mean F_ROH per Site map merges roh_per_ind with indpopdata Site coordinates."""
    inputs = {
        "per_ind": f"results/{wildcards.project}/roh/{wildcards.project}.roh_per_ind.txt",
        "indpopdata": f"results/{wildcards.project}/indpopdata.txt",
        "install": ".snakemake/mapmixture_installed",
    }
    inputs.update(_map_background_inputs(wildcards))
    return inputs


def _roh_group_by_columns_from_params(params):
    """Metadata columns configured for ROH group summaries and plots."""
    roh = params.get("roh", {})
    group_by = roh.get("group_by", ["Site"])
    if isinstance(group_by, list):
        return [
            str(x)
            for x in group_by
            if x and str(x).strip().lower() != "none"
        ]
    if isinstance(group_by, str) and group_by.strip().lower() != "none":
        return [group_by.strip()]
    return []


def _roh_group_setting(project, group_col, key, default=None):
    """Read per-group ROH settings from roh.{group_col}.{key}, with legacy roh.colors fallback."""
    roh = config["projects"][project]["parameters"].get("roh", {})
    group_cfg = roh.get(group_col)
    if isinstance(group_cfg, dict):
        return group_cfg.get(key, default)
    if key == "colors":
        return roh.get("colors", {}).get(group_col, default)
    return default


def _neighbornet_group_setting(project, group_col, key, default=None):
    """Read per-group NeighborNet settings from neighbornet.{group_col}.{key}, with legacy fallbacks."""
    nn = config["projects"][project]["parameters"].get("neighbornet", {})
    group_cfg = nn.get(group_col)
    if isinstance(group_cfg, dict):
        return group_cfg.get(key, default)
    if key == "colors":
        legacy = nn.get("neigbournet_colors") or nn.get("neighbornet_colors")
        if legacy:
            return legacy
        return nn.get("colors", {}).get(group_col, default)
    return default


def _pixy_group_setting(project, group_col, key, default=None):
    """Read per-group pixy settings from pixy.{group_col}.{key}, with legacy pixy.colors fallback."""
    pixy = config["projects"][project]["parameters"].get("pixy", {})
    group_cfg = pixy.get(group_col)
    if isinstance(group_cfg, dict):
        return group_cfg.get(key, default)
    if key == "colors":
        return pixy.get("colors", {}).get(group_col, default)
    return default


def _roh_group_by_columns(wildcards):
    params = config["projects"][wildcards.project]["parameters"]
    return _roh_group_by_columns_from_params(params)


def _roh_plot_artifacts(plot_dir, project, stem):
    """PDF + ggplot RDS paths for one ROH plot stem."""
    base = f"{plot_dir}/{project}.{stem}"
    return [f"{base}.pdf", f"{base}.rds"]


def _roh_plot_stems(group_by_cols):
    """Plot filename stems for one ROH plots rule."""
    stems = [
        "froh_histogram",
        "nroh_histogram",
        "length_histogram",
        "length_segments_histogram",
        "froh_classes",
        "froh_classes_histogram",
        "nroh_classes",
        "nroh_classes_histogram",
        "froh_vs_nroh",
    ]
    for col in group_by_cols:
        stems.extend([
            f"froh_by_{col}",
            f"froh_by_{col}_classes",
            f"nroh_by_{col}",
            f"nroh_by_{col}_classes",
        ])
    return stems


def _roh_plot_paths(project, group_by_cols):
    """All ROH plot paths (fixed plots + group_by expansions)."""
    plot_dir = f"results/{project}/roh/plots"
    paths = []
    for stem in _roh_plot_stems(group_by_cols):
        paths.extend(_roh_plot_artifacts(plot_dir, project, stem))
    return paths


def _froh_map_params(wildcards):
    """Params for plot_froh_map (map_background + roh map knobs)."""
    roh = config["projects"][wildcards.project]["parameters"].get("roh", {})
    return {
        **_map_bg_params(wildcards),
        "point_size": roh.get("point_size", 3),
        "map_outline": roh.get("map_outline", True),
    }


def _normalize_max_overlaps(value):
    """Return a Snakemake/R-safe value for ggrepel max.overlaps (never Python float inf)."""
    if value is None:
        return ".inf"
    if isinstance(value, float) and value == float("inf"):
        return ".inf"
    if isinstance(value, str) and value.lower() in ("inf", "infinity", ".inf"):
        return ".inf"
    return value


def _population_map_params(wildcards):
    """Params for plot_population_map (map_background + label/ggrepel knobs)."""
    pm = config["projects"][wildcards.project]["parameters"]["population_map"]
    return {
        **_map_bg_params(wildcards),
        "point_size": pm.get("point_size", 3),
        "point_color": pm.get("point_color", "black"),
        "point_shape": pm.get("point_shape", 19),
        "label_size": pm.get("label_size", 3),
        "label_color": pm.get("label_color", "black"),
        "label_fontface": pm.get("label_fontface", "plain"),
        "show_points": pm.get("show_points", True),
        "show_labels": pm.get("show_labels", True),
        "force": pm.get("force", 10),
        "force_pull": pm.get("force_pull", 1),
        "max_overlaps": _normalize_max_overlaps(pm.get("max.overlaps", ".inf")),
        "min_segment_length": pm.get("min.segment.length", 0.5),
        "segment_color": pm.get("segment.color", "grey50"),
        "segment_size": pm.get("segment.size", 0.5),
    }


def _spca_map_rule_params(wildcards):
    """Params for spca_score_map (map_background + sPCA map knobs)."""
    spca_map = config["projects"][wildcards.project]["parameters"].get("spca_map", {})
    return {
        **_map_bg_params(wildcards),
        "score_type": wildcards.score_type,
        "axis": int(wildcards.axis),
        "point_size": spca_map.get("point_size", 2.5),
        "point_alpha": spca_map.get("point_alpha", 0.9),
        "low_colour": spca_map.get("low_colour", "#2166AC"),
        "mid_colour": spca_map.get("mid_colour", "white"),
        "high_colour": spca_map.get("high_colour", "#B2182B"),
    }


def _tess3_map_resolution(wildcards):
    """plot.tess3Q/ggtess3Q interpolation grid c(rx, ry) from map_background size."""
    bg = config["projects"][wildcards.project]["parameters"]["map_background"]
    width = float(bg.get("width", 10))
    height = float(bg.get("height", 8))
    dpi = float(bg.get("dpi", 300))
    # 30 cells per inch of plot extent (dpi/10); matches legacy [300, 300] at 10×10 in, 300 dpi
    scale = dpi / 10.0
    return [
        max(50, int(round(width * scale))),
        max(50, int(round(height * scale))),
    ]


def _tess3_ggmap_params(wildcards):
    """Params for plot_tess3_ggmap (map_background + tess3 ggmap knobs)."""
    mm = config["projects"][wildcards.project]["parameters"]["mapmixture"]
    return {
        **_map_bg_params(wildcards),
        "k": int(wildcards.k),
        "map_resolution": _tess3_map_resolution(wildcards),
        "interpolation_knots": _tess3_params(wildcards).get("interpolation_knots", 10),
        "point_size": _tess3_params(wildcards).get("ggmap_point_size", 0.8),
        "tile_alpha": _tess3_params(wildcards).get("ggmap_tile_alpha", 1.0),
        "structure_colors": mm.get("structure_colors", _DEFAULT_STRUCTURE_COLORS),
    }


# OrientAGraph/TreeMix helper functions — defined here so get_all_targets() can
# call them without a forward dependency on the orientagraph.smk include.

def _treemix_migration_edges_from_params(params):
    tm = params.get("treemix", {})
    edges = tm.get("migration_edges", [0])
    if isinstance(edges, int):
        edges = [edges]
    return [int(edge) for edge in edges]


def _treemix_bootstrap_config_from_params(params):
    tm = params.get("treemix", {})
    bootstrap = tm.get("bootstrap", {})
    if isinstance(bootstrap, bool):
        return {"enabled": bootstrap, "migration_edges": [], "replicates": 100}
    return bootstrap or {}


def _treemix_bootstrap_enabled_from_params(params):
    return bool(_treemix_bootstrap_config_from_params(params).get("enabled", False))


# Include rule files
include: "rules/preprocessing.smk"
include: "rules/preprocessing_ld_pruning.smk"
include: "rules/preprocessing_exports.smk"
include: "rules/popdata.smk"
include: "rules/missingness.smk"
include: "rules/vcfstats.smk"
include: "rules/faststructure.smk"
include: "rules/admixture.smk"
include: "rules/pcaone.smk"
include: "rules/vcf2pcacluster.smk"
include: "rules/relatedness.smk"
include: "rules/pixy.smk"
include: "rules/structure.smk"
include: "rules/iqtree.smk"
include: "rules/mapmixture.smk"
include: "rules/map_facet.smk"
include: "rules/population_map.smk"
include: "rules/genetic_distances.smk"
include: "rules/neighbornet.smk"
include: "rules/pcoa.smk"
include: "rules/mapi.smk"
include: "rules/fineradstructure.smk"
include: "rules/dapc.smk"
include: "rules/construct.smk"
include: "rules/tess3.smk"
include: "rules/snmf.smk"
include: "rules/alstructure.smk"
include: "rules/spca.smk"
include: "rules/evaladmix.smk"
include: "rules/genome_scan.smk"
include: "rules/amova.smk"
include: "rules/roh.smk"
include: "rules/iqtree_trimal.smk"
include: "rules/njtree.smk"
include: "rules/orientagraph.smk"
include: "rules/methods.smk"

def get_all_targets():
    """
    Build list of target files based on enabled analyses in config.
    This keeps the main Snakefile clean and makes it easy to 
    enable/disable analyses by editing the config file.
    Supports multiple projects, each with their own analyses.
    """
    targets = []

    # Iterate through each project
    for project in config["projects"].keys():
        project_config = config["projects"][project]
        analyses = project_config.get("analyses", {})
        params = project_config.get("parameters", {})

        # Methods document — always generated
        targets.append(f"results/{project}/methods.md")

        # Core QC/statistics outputs — always generated for each project.
        targets.extend([
            f"results/{project}/indpopdata.txt",
            f"results/{project}/indpopdata_all.txt",
            f"results/{project}/stats_samples/{project}.population_summary.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.vcf_stats.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.vcf_stats.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.vcf_stats.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.idepth",
            f"results/{project}/stats_vcf/original/{project}.subset.ldepth.mean",
            f"results/{project}/stats_vcf/original/{project}.subset.depth_summary.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.idepth_summary.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.ldepth_summary.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.idepth",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.ldepth.mean",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.depth_summary.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.idepth_summary.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.ldepth_summary.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.idepth",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.ldepth.mean",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.depth_summary.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.idepth_summary.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.ldepth_summary.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.chromosome_stats.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.chromosome_stats.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.chromosome_stats.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.imiss",
            f"results/{project}/stats_vcf/original/{project}.subset.lmiss",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.imiss",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.lmiss",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.imiss",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.lmiss",
            f"results/{project}/stats_vcf/original/{project}.subset.imiss_summary.txt",
            f"results/{project}/stats_vcf/original/{project}.subset.lmiss_summary.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.imiss_summary.txt",
            f"results/{project}/stats_vcf/filtered/{project}.filtered.lmiss_summary.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.imiss_summary.txt",
            f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.lmiss_summary.txt",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.imiss_histogram.pdf",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.imiss_histogram.rds",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.lmiss_histogram.pdf",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.lmiss_histogram.rds",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.idepth_histogram.pdf",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.idepth_histogram.rds",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.ldepth_histogram.pdf",
            f"results/{project}/stats_vcf/original/plots/{project}.subset.ldepth_histogram.rds",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.imiss_histogram.pdf",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.imiss_histogram.rds",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.lmiss_histogram.pdf",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.lmiss_histogram.rds",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.idepth_histogram.pdf",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.idepth_histogram.rds",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.ldepth_histogram.pdf",
            f"results/{project}/stats_vcf/filtered/plots/{project}.filtered.ldepth_histogram.rds",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.imiss_histogram.pdf",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.imiss_histogram.rds",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.lmiss_histogram.pdf",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.lmiss_histogram.rds",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.idepth_histogram.pdf",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.idepth_histogram.rds",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.ldepth_histogram.pdf",
            f"results/{project}/stats_vcf/thinned/plots/{project}.biallelic_snps.ldepth_histogram.rds",
        ])

        # Get project-specific parameters
        k_values = params.get("k_values", [1, 2, 3])
        plot_k_values = _plot_k_values(k_values)
        n_reps = params.get("structure", {}).get("replicates", 1)
        # Default to ["none"] if color_by not specified - will still generate all 6 plots (gray, labeled, missing)
        PCA_groupings = params.get("pca_plot", {}).get("color_by", ["none"])
        pc_max = params.get("pca_plot", {}).get("pc_max", 2)
        pca_combinations = get_pca_combinations(pc_max)
        
        # FastStructure targets
        if analyses.get("faststructure", False):
            targets.extend([
                expand(f"results/{project}/faststructure/{project}.faststructure.{{k}}.meanQ",
                       k=k_values),
                expand(f"results/{project}/faststructure/{project}.faststructure.{{k}}.meanP",
                       k=k_values),
                f"results/{project}/faststructure/{project}.faststructure.chooseK_results.txt"
            ])
            # Only add map plots if popdata is specified
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/faststructure/plots/{project}.faststructure.K{{k}}.map.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/faststructure/plots/{project}.faststructure.K{{k}}.map.rds",
                           k=plot_k_values)
                ])
                if plot_k_values:
                    targets.extend(_map_facet_targets(project, "faststructure"))
            targets.extend([
                expand(f"results/{project}/faststructure/plots/{project}.faststructure.K{{k}}.barplot.pdf",
                       k=plot_k_values),
                expand(f"results/{project}/faststructure/plots/{project}.faststructure.K{{k}}.barplot.rds",
                       k=plot_k_values)
            ])

        # ADMIXTURE targets
        if analyses.get("admixture", False):
            targets.extend([
                expand(f"results/{project}/admixture/{project}.biallelic_snps_thinned.{{k}}.Q",
                       k=k_values),
                expand(f"results/{project}/admixture/{project}.biallelic_snps_thinned.{{k}}.P",
                       k=k_values),
                f"results/{project}/admixture/{project}.admixture.chooseK_results.txt",
                f"results/{project}/admixture/{project}.admixture.cv_summary.txt",
                f"results/{project}/admixture/plots/{project}.admixture.cv_plot.pdf",
                f"results/{project}/admixture/plots/{project}.admixture.cv_plot.rds",
            ])
            # Only add map plots if popdata is specified
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/admixture/plots/{project}.admixture.K{{k}}.map.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/admixture/plots/{project}.admixture.K{{k}}.map.rds",
                           k=plot_k_values)
                ])
                if plot_k_values:
                    targets.extend(_map_facet_targets(project, "admixture"))
            targets.extend([
                expand(f"results/{project}/admixture/plots/{project}.admixture.K{{k}}.barplot.pdf",
                       k=plot_k_values),
                expand(f"results/{project}/admixture/plots/{project}.admixture.K{{k}}.barplot.rds",
                       k=plot_k_values)
            ])

        # STRUCTURE targets
        if analyses.get("structure", False):
            targets.extend([
                expand(f"results/{project}/structure/{project}.structure.K{{k}}.R{{r}}_f",
                       k=k_values,
                       r=range(1, n_reps + 1)),
                f"results/{project}/structure/{project}.K_aligned.pdf",
                expand(f"results/{project}/structure/{project}.structure.K{{k}}.Qmatrix.txt",
                       k=k_values),
                f"results/{project}/structure/plots/{project}.evanno_plot.pdf",
                f"results/{project}/structure/plots/{project}.evanno_plot.rds"
            ])
            # Only add map plots if popdata is specified
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/structure/plots/{project}.structure.K{{k}}.map.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/structure/plots/{project}.structure.K{{k}}.map.rds",
                           k=plot_k_values)
                ])
                if plot_k_values:
                    targets.extend(_map_facet_targets(project, "structure"))
            targets.extend([
                expand(f"results/{project}/structure/plots/{project}.structure.K{{k}}.barplot.pdf",
                       k=plot_k_values),
                expand(f"results/{project}/structure/plots/{project}.structure.K{{k}}.barplot.rds",
                       k=plot_k_values)
            ])

        # tess3r targets
        if analyses.get("tess3", False):
            if _has_popdata(params):
                targets.extend([
                    f"results/{project}/tess3/{project}.tess3.chooseK_results.txt",
                    f"results/{project}/tess3/{project}.tess3.cv_summary.txt",
                    f"results/{project}/tess3/plots/{project}.tess3.cv_plot.pdf",
                    f"results/{project}/tess3/plots/{project}.tess3.cv_plot.rds",
                    expand(f"results/{project}/tess3/{project}.tess3.K{{k}}.Qmatrix.txt",
                           k=k_values),
                    expand(f"results/{project}/tess3/{project}.tess3.K{{k}}.results.rds",
                           k=k_values),
                    expand(f"results/{project}/tess3/{project}.tess3.K{{k}}.cross_entropy.txt",
                           k=k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.max_cluster.png",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.interpolation.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.ggmap.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.ggmap.rds",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.map.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.map.rds",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.barplot.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/tess3/plots/{project}.tess3.K{{k}}.barplot.rds",
                           k=plot_k_values)
                ])
                if plot_k_values:
                    targets.extend(_map_facet_targets(project, "tess3"))

        # sNMF (LEA) targets
        if analyses.get("snmf", False):
            targets.extend([
                f"results/{project}/snmf/{project}.snmf.chooseK_results.txt",
                f"results/{project}/snmf/{project}.snmf.cv_summary.txt",
                f"results/{project}/snmf/plots/{project}.snmf.cv_plot.pdf",
                f"results/{project}/snmf/plots/{project}.snmf.cv_plot.rds",
                expand(f"results/{project}/snmf/{project}.snmf.K{{k}}.Qmatrix.txt",
                       k=k_values),
                expand(f"results/{project}/snmf/{project}.snmf.K{{k}}.cross_entropy.txt",
                       k=k_values),
                expand(f"results/{project}/snmf/{project}.snmf.K{{k}}.results.rds",
                       k=k_values),
            ])
            # Only add map plots if popdata is specified
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/snmf/plots/{project}.snmf.K{{k}}.map.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/snmf/plots/{project}.snmf.K{{k}}.map.rds",
                           k=plot_k_values)
                ])
                if plot_k_values:
                    targets.extend(_map_facet_targets(project, "snmf"))
            targets.extend([
                expand(f"results/{project}/snmf/plots/{project}.snmf.K{{k}}.barplot.pdf",
                       k=plot_k_values),
                expand(f"results/{project}/snmf/plots/{project}.snmf.K{{k}}.barplot.rds",
                       k=plot_k_values)
            ])

        # ALStructure targets
        if analyses.get("alstructure", False):
            # ALStructure requires at least 2 latent populations (estimate_d enforces
            # d >= 2 and estimate_F collapses at d = 1); skip K = 1 like DAPC.
            alstructure_k_values = plot_k_values
            targets.extend([
                f"results/{project}/alstructure/{project}.alstructure.chooseK_results.txt",
                f"results/{project}/alstructure/{project}.alstructure.cv_summary.txt",
                f"results/{project}/alstructure/plots/{project}.alstructure.cv_plot.pdf",
                f"results/{project}/alstructure/plots/{project}.alstructure.cv_plot.rds",
                expand(f"results/{project}/alstructure/{project}.alstructure.K{{k}}.Qmatrix.txt",
                       k=alstructure_k_values),
                expand(f"results/{project}/alstructure/{project}.alstructure.K{{k}}.results.rds",
                       k=alstructure_k_values),
            ])
            # Only add map plots if popdata is specified
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/alstructure/plots/{project}.alstructure.K{{k}}.map.pdf",
                           k=alstructure_k_values),
                    expand(f"results/{project}/alstructure/plots/{project}.alstructure.K{{k}}.map.rds",
                           k=alstructure_k_values)
                ])
                if alstructure_k_values:
                    targets.extend(_map_facet_targets(project, "alstructure"))
            # Always add barplots
            targets.extend([
                expand(f"results/{project}/alstructure/plots/{project}.alstructure.K{{k}}.barplot.pdf",
                       k=alstructure_k_values),
                expand(f"results/{project}/alstructure/plots/{project}.alstructure.K{{k}}.barplot.rds",
                       k=alstructure_k_values)
            ])

        # PCAone targets (standard)
        if analyses.get("pcaone", False):
            targets.extend([
                f"results/{project}/pcaone/{project}.PCA.eigvecs",
                f"results/{project}/pcaone/{project}.PCA.eigvals"
            ])
            # Always add missing data calculation target
            targets.append(f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.imiss")
            # Add plot targets for each PC combination and color grouping
            for pc1, pc2 in pca_combinations:
                for color_by in PCA_groupings:
                    # Request colored plots (uses color_by)
                    targets.extend([
                        f"results/{project}/pcaone/plots/{project}.PCA-PC{pc1}_PC{pc2}-{color_by}.pdf",
                        f"results/{project}/pcaone/plots/{project}.PCA-PC{pc1}_PC{pc2}-{color_by}.rds"
                    ])
                # Add labeled and missing plots once per PC combination (no color_by)
                targets.extend([
                    f"results/{project}/pcaone/plots/{project}.PCA-PC{pc1}_PC{pc2}-labeled.pdf",
                    f"results/{project}/pcaone/plots/{project}.PCA-PC{pc1}_PC{pc2}-labeled.rds",
                    f"results/{project}/pcaone/plots/{project}.PCA-PC{pc1}_PC{pc2}-missing.pdf",
                    f"results/{project}/pcaone/plots/{project}.PCA-PC{pc1}_PC{pc2}-missing.rds"
                ])
            # Add facet plots (all PC combinations in one plot)
            for color_by in PCA_groupings:
                targets.extend([
                    f"results/{project}/pcaone/plots/{project}.PCA-facet-{color_by}.pdf",
                    f"results/{project}/pcaone/plots/{project}.PCA-facet-{color_by}.rds"
                ])
            targets.extend([
                f"results/{project}/pcaone/plots/{project}.PCA-facet-labeled.pdf",
                f"results/{project}/pcaone/plots/{project}.PCA-facet-labeled.rds",
                f"results/{project}/pcaone/plots/{project}.PCA-facet-missing.pdf",
                f"results/{project}/pcaone/plots/{project}.PCA-facet-missing.rds"
            ])

        # PCAone targets (EMU)
        if analyses.get("pcaone_emu", False):
            targets.extend([
                f"results/{project}/pcaone_EMU/{project}.PCA_EMU.eigvecs",
                f"results/{project}/pcaone_EMU/{project}.PCA_EMU.eigvals"
            ])
            # Always add missing data calculation target (reuses same .imiss as pcaone)
            targets.append(f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.imiss")
            # Add plot targets for each PC combination and color grouping
            for pc1, pc2 in pca_combinations:
                for color_by in PCA_groupings:
                    # Request colored plots (uses color_by)
                    targets.extend([
                        f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-PC{pc1}_PC{pc2}-{color_by}.pdf",
                        f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-PC{pc1}_PC{pc2}-{color_by}.rds"
                    ])
                # Add labeled and missing plots once per PC combination (no color_by)
                targets.extend([
                    f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-PC{pc1}_PC{pc2}-labeled.pdf",
                    f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-PC{pc1}_PC{pc2}-labeled.rds",
                    f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-PC{pc1}_PC{pc2}-missing.pdf",
                    f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-PC{pc1}_PC{pc2}-missing.rds"
                ])
            # Add facet plots (all PC combinations in one plot)
            for color_by in PCA_groupings:
                targets.extend([
                    f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-facet-{color_by}.pdf",
                    f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-facet-{color_by}.rds"
                ])
            targets.extend([
                f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-facet-labeled.pdf",
                f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-facet-labeled.rds",
                f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-facet-missing.pdf",
                f"results/{project}/pcaone_EMU/plots/{project}.PCA_EMU-facet-missing.rds"
            ])

        # PCAone targets (with coverage thresholds)
        if analyses.get("pcaone_miss", False):
            miss_values = params.get("PCAone", {}).get("miss", [0.5])
            targets.extend([
                expand(f"results/{project}/pcaone_miss{{miss}}/{project}.PCA_miss{{miss}}.eigvecs",
                       miss=miss_values),
                expand(f"results/{project}/pcaone_miss{{miss}}/{project}.PCA_miss{{miss}}.eigvals",
                       miss=miss_values)
            ])
            # Always add missing data calculation targets for each threshold
            targets.extend([
                expand(f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps_thinned_miss{{miss}}.imiss",
                       miss=miss_values)
            ])
            # Add plot targets for each miss value, PC combination, and color grouping
            for miss in miss_values:
                for pc1, pc2 in pca_combinations:
                    for color_by in PCA_groupings:
                        # Request colored plots (uses color_by)
                        targets.extend([
                            f"results/{project}/pcaone_miss{miss}/plots/{project}.PCA_miss{miss}-PC{pc1}_PC{pc2}-{color_by}.pdf",
                            f"results/{project}/pcaone_miss{miss}/plots/{project}.PCA_miss{miss}-PC{pc1}_PC{pc2}-{color_by}.rds"
                        ])
                    # Add labeled and missing plots once per PC combination (no color_by)
                    targets.extend([
                        f"results/{project}/pcaone_miss{miss}/plots/{project}.PCA_miss{miss}-PC{pc1}_PC{pc2}-labeled.pdf",
                        f"results/{project}/pcaone_miss{miss}/plots/{project}.PCA_miss{miss}-PC{pc1}_PC{pc2}-labeled.rds",
                        f"results/{project}/pcaone_miss{miss}/plots/{project}.PCA_miss{miss}-PC{pc1}_PC{pc2}-missing.pdf",
                        f"results/{project}/pcaone_miss{miss}/plots/{project}.PCA_miss{miss}-PC{pc1}_PC{pc2}-missing.rds"
                    ])
        
        # VCF2PCACluster targets
        if analyses.get("vcf2pcacluster", False):
            vcf2pca_params = params.get("vcf2pcacluster", {}).get("SNP_filtering", {})
            miss_values = vcf2pca_params.get("Miss", [0.5])
            maf_values = vcf2pca_params.get("MAF", [0])

            targets.extend(
                expand(
                    f"results/{project}/vcf2pcacluster_miss{miss}_MAF{MAF}/{project}.vcf2pcacluster_miss{miss}_MAF{MAF}.eigenvec",
                    miss=miss_values,
                    MAF=maf_values
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/vcf2pcacluster_miss{miss}_MAF{MAF}/{project}.vcf2pcacluster_miss{miss}_MAF{MAF}.eigenval",
                    miss=miss_values,
                    MAF=maf_values
                )
            )

        # Pixy: summaries, plot_pixy_* outputs (barplots, heatmaps, optional π map)
        if analyses.get("pixy", False):
            pixy_groupings = _pixy_grouping_columns(params)
            pixy_pairwise_groupings = _pixy_groupings_with_min_populations(
                project, params
            )
            pixy_stats = _pixy_enabled_stats(params)
            if "pi" in pixy_stats:
                targets.extend(
                    expand(
                        f"results/{project}/pixy/{project}.{{grouping}}.pixy_pi-summary.txt",
                        grouping=pixy_groupings,
                    )
                )
                targets.extend(
                    expand(
                        f"results/{project}/pixy/plots/{project}.{{grouping}}.pixy_pi.pdf",
                        grouping=pixy_groupings,
                    )
                )
                targets.extend(
                    expand(
                        f"results/{project}/pixy/plots/{project}.{{grouping}}.pixy_pi.rds",
                        grouping=pixy_groupings,
                    )
                )
                # π map uses Site-level summary only (merge to geographic Site coordinates)
                if "Site" in pixy_groupings:
                    if _has_popdata(params):
                        targets.extend(
                            [
                                f"results/{project}/pixy/plots/{project}.pixy_pi_map.pdf",
                                f"results/{project}/pixy/plots/{project}.pixy_pi_map.rds",
                            ]
                        )
            if "fst" in pixy_stats:
                targets.extend(
                    expand(
                        f"results/{project}/pixy/{project}.{{grouping}}.pixy_fst-summary.txt",
                        grouping=pixy_pairwise_groupings,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/pixy/plots/"
                        + project
                        + ".{grouping}.pixy_fst_heatmap.pdf",
                        grouping=pixy_pairwise_groupings,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/pixy/plots/"
                        + project
                        + ".{grouping}.pixy_fst_heatmap.rds",
                        grouping=pixy_pairwise_groupings,
                    )
                )
            if "dxy" in pixy_stats:
                targets.extend(
                    expand(
                        f"results/{project}/pixy/{project}.{{grouping}}.pixy_dxy-summary.txt",
                        grouping=pixy_pairwise_groupings,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/pixy/plots/"
                        + project
                        + ".{grouping}.pixy_dxy_heatmap.pdf",
                        grouping=pixy_pairwise_groupings,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/pixy/plots/"
                        + project
                        + ".{grouping}.pixy_dxy_heatmap.rds",
                        grouping=pixy_pairwise_groupings,
                    )
                )

        # Relatedness targets
        if analyses.get("relatedness", False):
            relatedness_color_by = _relatedness_color_by_columns(params)
            targets.extend([
                f"results/{project}/relatedness/{project}.relatedness",
                f"results/{project}/relatedness/{project}.relatedness2",
                f"results/{project}/relatedness/{project}.genome",
                f"results/{project}/relatedness/{project}.king"
            ])
            targets.extend(
                expand(
                    f"results/{project}/relatedness/plots/{project}.genome_network-{{color_by}}.pdf",
                    color_by=relatedness_color_by,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/relatedness/plots/{project}.genome_network-{{color_by}}.rds",
                    color_by=relatedness_color_by,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/relatedness/plots/{project}.king_network-{{color_by}}.pdf",
                    color_by=relatedness_color_by,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/relatedness/plots/{project}.king_network-{{color_by}}.rds",
                    color_by=relatedness_color_by,
                )
            )
        
        # IQtree targets
        if analyses.get("iqtree", False):
            targets.extend([
                f"results/{project}/iqtree/{project}.iqtree.treefile",
                f"results/{project}/iqtree/{project}.iqtree.iqtree",
                f"results/{project}/iqtree/{project}.iqtree.log",
                f"results/{project}/iqtree/plots/{project}.tree_plot.unrooted.pdf",
                f"results/{project}/iqtree/plots/{project}.tree_plot.unrooted.rds",
                f"results/{project}/iqtree/plots/{project}.tree_plot.rooted.pdf",
                f"results/{project}/iqtree/plots/{project}.tree_plot.rooted.rds",
                f"results/{project}/iqtree/{project}.iqtree.unrooted.treefile",
                f"results/{project}/iqtree/{project}.iqtree.rooted.treefile"
            ])

        # IQtree robust targets
        if analyses.get("iqtree_robust", False):
            targets.extend([
                f"results/{project}/iqtree_robust/{project}.iqtree_robust.treefile",
                f"results/{project}/iqtree_robust/{project}.iqtree_robust.iqtree",
                f"results/{project}/iqtree_robust/{project}.iqtree_robust.log",
                f"results/{project}/iqtree_robust/plots/{project}.tree_plot.unrooted.pdf",
                f"results/{project}/iqtree_robust/plots/{project}.tree_plot.unrooted.rds",
                f"results/{project}/iqtree_robust/plots/{project}.tree_plot.rooted.pdf",
                f"results/{project}/iqtree_robust/plots/{project}.tree_plot.rooted.rds",
                f"results/{project}/iqtree_robust/{project}.iqtree_robust.unrooted.treefile",
                f"results/{project}/iqtree_robust/{project}.iqtree_robust.rooted.treefile"
            ])

        # NJ tree targets (rapidNJ from ipyrad PHYLIP alignment)
        if analyses.get("njtree", False):
            targets.extend([
                f"results/{project}/njtree/{project}.njtree.treefile",
                f"results/{project}/njtree/{project}.njtree.log",
                f"results/{project}/njtree/plots/{project}.tree_plot.unrooted.pdf",
                f"results/{project}/njtree/plots/{project}.tree_plot.unrooted.rds",
                f"results/{project}/njtree/{project}.njtree.unrooted.treefile",
            ])

        # IQ-TREE after trimAl targets
        if analyses.get("iqtree_trimal", False):
            targets.extend([
                f"results/{project}/filtered_data/{project}.trimmed.phy",
                f"results/{project}/iqtree_trimal/{project}.iqtree.treefile",
                f"results/{project}/iqtree_trimal/{project}.iqtree.iqtree",
                f"results/{project}/iqtree_trimal/{project}.iqtree.log",
                f"results/{project}/iqtree_trimal/plots/{project}.tree_plot.unrooted.pdf",
                f"results/{project}/iqtree_trimal/plots/{project}.tree_plot.unrooted.rds",
                f"results/{project}/iqtree_trimal/plots/{project}.tree_plot.rooted.pdf",
                f"results/{project}/iqtree_trimal/plots/{project}.tree_plot.rooted.rds",
                f"results/{project}/iqtree_trimal/{project}.iqtree.unrooted.treefile",
                f"results/{project}/iqtree_trimal/{project}.iqtree.rooted.treefile",
            ])

        # evalAdmix targets (requires matching ancestry inference)
        if analyses.get("evaladmix", False):
            if analyses.get("admixture", False):
                targets.extend(
                    expand(
                        f"results/{project}/admixture/evaladmix/{project}.admixture.K{k}.corres",
                        k=k_values,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/admixture/evaladmix/plots/"
                        + project
                        + ".admixture.K{k}.evaladmix.pdf",
                        k=k_values,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/admixture/evaladmix/plots/"
                        + project
                        + ".admixture.K{k}.evaladmix.rds",
                        k=k_values,
                    )
                )
            if analyses.get("faststructure", False):
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/faststructure/evaladmix/"
                        + project
                        + ".faststructure.K{k}.corres",
                        k=k_values,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/faststructure/evaladmix/plots/"
                        + project
                        + ".faststructure.K{k}.evaladmix.pdf",
                        k=k_values,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/faststructure/evaladmix/plots/"
                        + project
                        + ".faststructure.K{k}.evaladmix.rds",
                        k=k_values,
                    )
                )
            if analyses.get("structure", False):
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/structure/evaladmix/"
                        + project
                        + ".structure.K{k}.corres",
                        k=k_values,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/structure/evaladmix/plots/"
                        + project
                        + ".structure.K{k}.evaladmix.pdf",
                        k=k_values,
                    )
                )
                targets.extend(
                    expand(
                        "results/"
                        + project
                        + "/structure/evaladmix/plots/"
                        + project
                        + ".structure.K{k}.evaladmix.rds",
                        k=k_values,
                    )
                )

        # Genome scan targets (requires pop1 and pop2 in config)
        if analyses.get("genome_scan", False):
            gs_cfg = params.get("genome_scan", {})
            if gs_cfg.get("pop1") and gs_cfg.get("pop2"):
                targets.extend([
                    f"results/{project}/genome_scan/{project}.genome_scan_fst.txt",
                    f"results/{project}/genome_scan/{project}.genome_scan_pi.txt",
                    f"results/{project}/genome_scan/{project}.genome_scan_dxy.txt",
                    "results/"
                    + project
                    + "/genome_scan/plots/"
                    + project
                    + ".genome_scan_genome_plot.pdf",
                    "results/"
                    + project
                    + "/genome_scan/plots/"
                    + project
                    + ".genome_scan_genome_plot.rds",
                    "results/"
                    + project
                    + "/genome_scan/plots/"
                    + project
                    + ".genome_scan.imiss_histogram.pdf",
                    "results/"
                    + project
                    + "/genome_scan/plots/"
                    + project
                    + ".genome_scan.lmiss_histogram.pdf",
                ])

        # AMOVA targets (requires non-empty strata)
        if analyses.get("amova", False):
            amova_strata = params.get("amova", {}).get("strata", [])
            if amova_strata:
                targets.extend([
                    f"results/{project}/amova/{project}.amova_results.txt",
                    "results/"
                    + project
                    + "/amova/plots/"
                    + project
                    + ".amova_variance_components.pdf",
                    "results/"
                    + project
                    + "/amova/plots/"
                    + project
                    + ".amova_variance_components.rds",
                ])

        # ROH targets
        if analyses.get("roh", False):
            roh_groupings = _roh_group_by_columns_from_params(params)
            targets.extend([
                f"results/{project}/roh/{project}.bcftools_roh.txt",
                f"results/{project}/roh/{project}.roh_summary.txt",
                f"results/{project}/roh/{project}.roh_per_ind.txt",
                f"results/{project}/roh/{project}.roh_stats_comparisons.txt",
            ])
            targets.extend(_roh_plot_paths(project, roh_groupings))
            if _has_popdata(params):
                targets.extend([
                    f"results/{project}/roh/plots/{project}.froh_map.pdf",
                    f"results/{project}/roh/plots/{project}.froh_map.rds",
                ])

        # Genetic distance targets
        if analyses.get("gen_dist", False):
            targets.extend([
                f"results/{project}/gen_dist/{project}.kosman_distance.tsv",
                f"results/{project}/gen_dist/{project}.euclidean_distance.tsv",
                f"results/{project}/gen_dist/{project}.p_distance.tsv",
                f"results/{project}/gen_dist/{project}.avgsquared_distance.tsv"
            ])

        # NeighborNet targets
        if analyses.get("neighbornet", False):
            neighbornet_color_by = _neighbornet_color_by_columns(params)
            targets.extend([
                f"results/{project}/gen_dist/{project}.p_distance.tsv",
                f"results/{project}/neighbornet/{project}.pdistance.neighbornet.rds",
            ])
            targets.extend(
                expand(
                    f"results/{project}/neighbornet/plots/{project}.pdistance.neighbornet-{{color_by}}.with_tip_labels.pdf",
                    color_by=neighbornet_color_by,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/neighbornet/plots/{project}.pdistance.neighbornet-{{color_by}}.no_tip_labels.pdf",
                    color_by=neighbornet_color_by,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/neighbornet/plots/{project}.pdistance.neighbornet-{{color_by}}.phangorn.pdf",
                    color_by=neighbornet_color_by,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/neighbornet/plots/{project}.pdistance.neighbornet-{{color_by}}.rds",
                    color_by=neighbornet_color_by,
                )
            )

        # PCoA targets
        if analyses.get("pcoa", False):
            pcoa_distances = ["euclidean", "kosman", "avgsquared"]
            # Always add missing data calculation target (used by PCoA plot rules)
            targets.append(f"results/{project}/stats_vcf/thinned/{project}.biallelic_snps.imiss")
            targets.extend(
                expand(
                    f"results/{project}/PCoA/{project}.PCoA_{{distance}}.eigvecs",
                    distance=pcoa_distances,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/PCoA/{project}.PCoA_{{distance}}.eigvals",
                    distance=pcoa_distances,
                )
            )
            # Add plot targets for each distance, PC combination, and color grouping
            for distance in pcoa_distances:
                for pc1, pc2 in pca_combinations:
                    for color_by in PCA_groupings:
                        targets.extend([
                            f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-PC{pc1}_PC{pc2}-{color_by}.pdf",
                            f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-PC{pc1}_PC{pc2}-{color_by}.rds"
                        ])
                    targets.extend([
                        f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-PC{pc1}_PC{pc2}-labeled.pdf",
                        f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-PC{pc1}_PC{pc2}-labeled.rds",
                        f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-PC{pc1}_PC{pc2}-missing.pdf",
                        f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-PC{pc1}_PC{pc2}-missing.rds"
                    ])
                for color_by in PCA_groupings:
                    targets.extend([
                        f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-facet-{color_by}.pdf",
                        f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-facet-{color_by}.rds"
                    ])
                targets.extend([
                    f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-facet-labeled.pdf",
                    f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-facet-labeled.rds",
                    f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-facet-missing.pdf",
                    f"results/{project}/PCoA/plots/{project}.PCoA_{distance}-facet-missing.rds"
                ])

        # MAPI targets
        if analyses.get("mapi", False):
            # Only run if popdata is specified (required for geographic coordinates)
            if _has_popdata(params):
                targets.extend([
                    f"results/{project}/gen_dist/{project}.euclidean_distance.tsv",
                    f"results/{project}/mapi/{project}.mapi_results.gpkg",
                    f"results/{project}/mapi/plots/{project}.mapi_euclidean.pdf",
                    f"results/{project}/mapi/plots/{project}.mapi_euclidean.rds",
                    f"results/{project}/mapi/{project}.mapi_upper_tails.gpkg",
                    f"results/{project}/mapi/{project}.mapi_lower_tails.gpkg"
                ])
        
        # Population map targets (always run if popdata is available)
        if _has_popdata(params):
            targets.extend([
                f"results/{project}/stats_samples/plots/{project}.population_map.pdf",
                f"results/{project}/stats_samples/plots/{project}.population_map.rds"
            ])

        # fineRADstructure targets
        if analyses.get("fineradstructure", False):
            targets.extend([
                f"results/{project}/fineradstructure/{project}.input",
                f"results/{project}/fineradstructure/{project}_chunks.out",
                f"results/{project}/fineradstructure/{project}_chunks.mcmc.xml",
                f"results/{project}/fineradstructure/{project}_chunks.mcmcTree.xml",
                f"results/{project}/fineradstructure/plots/{project}.SimpleCoancestry.pdf",
                f"results/{project}/fineradstructure/plots/{project}.PopAveragedCoancestry.pdf",
                f"results/{project}/fineradstructure/plots/{project}.PopAveragedCoancestryLabeled.pdf",
                f"results/{project}/fineradstructure/plots/{project}.fineradstructure_plots.rds",
            ])

        # TreeMix targets
        if analyses.get("treemix", False):
            treemix_migrations = _treemix_migration_edges_from_params(params)
            treemix_bootstrap_enabled = _treemix_bootstrap_enabled_from_params(params)
            targets.extend([
                f"results/{project}/treemix/{project}.treemix.frq.gz",
                f"results/{project}/treemix/{project}.treemix.clust",
                f"results/{project}/treemix/{project}.treemix.populations.tsv",
                f"results/{project}/treemix/{project}.treemix.positions.tsv",
                f"results/{project}/treemix/{project}.treemix.input_summary.tsv",
                f"results/{project}/treemix/{project}.treemix.migration_summary.tsv",
                f"results/{project}/treemix/{project}.treemix.optm_evanno.tsv",
                f"results/{project}/treemix/plots/{project}.treemix.likelihoods.pdf",
                f"results/{project}/treemix/plots/{project}.treemix.likelihoods.rds",
                f"results/{project}/treemix/plots/{project}.treemix.optm_evanno.pdf",
                f"results/{project}/treemix/plots/{project}.treemix.optm_evanno.rds",
            ])
            targets.extend(
                expand(
                    f"results/{project}/treemix/{project}.treemix.m{m}.llik",
                    m=treemix_migrations,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/treemix/{project}.treemix.m{m}.treeout.gz",
                    m=treemix_migrations,
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/treemix/plots/{project}.treemix.m{m}.{plot_type}.pdf",
                    m=treemix_migrations,
                    plot_type=["graph", "residuals"],
                )
            )
            targets.extend(
                expand(
                    f"results/{project}/treemix/plots/{project}.treemix.m{m}.{plot_type}.rds",
                    m=treemix_migrations,
                    plot_type=["graph", "residuals"],
                )
            )
            if treemix_bootstrap_enabled:
                targets.extend([
                    f"results/{project}/treemix/{project}.treemix.bootstrap_summary.tsv",
                    f"results/{project}/treemix/{project}.treemix.bootstrap_migration_edges.tsv",
                    f"results/{project}/treemix/plots/{project}.treemix.bootstrap_likelihoods.pdf",
                    f"results/{project}/treemix/plots/{project}.treemix.bootstrap_likelihoods.rds",
                ])

        # conStruct targets
        if analyses.get("construct", False):
            # Only run if popdata is specified (required for geographic coordinates)
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/construct/{project}.construct.K{{k}}.results.rds",
                           k=k_values),
                    expand(f"results/{project}/construct/{project}.construct.K{{k}}.layer_proportions.txt",
                           k=k_values),
                    expand(f"results/{project}/construct/plots/{project}.construct.K{{k}}.map.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/construct/plots/{project}.construct.K{{k}}.map.rds",
                           k=plot_k_values),
                    expand(f"results/{project}/construct/plots/{project}.construct.K{{k}}.barplot.pdf",
                           k=plot_k_values),
                    expand(f"results/{project}/construct/plots/{project}.construct.K{{k}}.barplot.rds",
                           k=plot_k_values),
                    f"results/{project}/construct/{project}.construct.chooseK_results.txt",
                    f"results/{project}/construct/{project}.construct.lpd_summary.txt",
                    f"results/{project}/construct/{project}.construct.layer_contribution_summary.txt",
                    f"results/{project}/construct/plots/{project}.construct.lpd_plot.pdf",
                    f"results/{project}/construct/plots/{project}.construct.lpd_plot.rds",
                    f"results/{project}/construct/plots/{project}.construct.layer_contribution_plot.pdf",
                    f"results/{project}/construct/plots/{project}.construct.layer_contribution_plot.rds",
                ])

        # sPCA targets
        if analyses.get("spca", False):
            if _has_popdata(params):
                n_spca_axes = _spca_n_axes_from_params(params)
                n_spca_global_axes = _spca_nfposi_from_params(params)
                n_spca_local_axes = _spca_nfnega_from_params(params)
                targets.extend([
                    f"results/{project}/spca/{project}.spca.results.rds",
                    f"results/{project}/spca/{project}.spca.scores.global.txt",
                    f"results/{project}/spca/{project}.spca.scores.local.txt",
                    f"results/{project}/spca/{project}.spca.eigenvalues.txt",
                    f"results/{project}/spca/{project}.spca.summary.txt",
                    f"results/{project}/spca/{project}.spca.global.rtest.txt",
                    f"results/{project}/spca/{project}.spca.local.rtest.txt",
                    f"results/{project}/spca/plots/{project}.spca.eig.barplot.pdf",
                    f"results/{project}/spca/plots/{project}.spca.screeplot.pdf",
                    f"results/{project}/spca/plots/{project}.spca.composite.pdf",
                    f"results/{project}/spca/plots/{project}.spca.map.pdf",
                    f"results/{project}/spca/plots/{project}.spca.colorplot.global.pdf",
                    f"results/{project}/spca/plots/{project}.spca.colorplot.local.pdf",
                    f"results/{project}/spca/plots/{project}.spca.global.rtest.pdf",
                    f"results/{project}/spca/plots/{project}.spca.local.rtest.pdf",
                    f"results/{project}/spca/plots/{project}.spca.scatter.global.pdf",
                    f"results/{project}/spca/plots/{project}.spca.scatter.local.pdf",
                ])
                targets.extend(
                    expand(
                        f"results/{project}/spca/plots/{project}.spca.axis{axis}.pdf",
                        axis=list(range(1, n_spca_axes + 1)),
                    )
                )
                targets.extend(
                    expand(
                        f"results/{project}/spca/plots/{project}.spca.loading.axis{axis}.pdf",
                        axis=list(range(1, n_spca_axes + 1)),
                    )
                )
                targets.extend(
                    expand(
                        f"results/{project}/spca/plots/{project}.spca.{score_type}_axis{axis}.map.pdf",
                        score_type=["global"],
                        axis=list(range(1, n_spca_global_axes + 1)),
                    )
                )
                targets.extend(
                    expand(
                        f"results/{project}/spca/plots/{project}.spca.{score_type}_axis{axis}.map.pdf",
                        score_type=["local"],
                        axis=list(range(1, n_spca_local_axes + 1)),
                    )
                )

        # DAPC targets
        if analyses.get("dapc", False):
            dapc_k_values = plot_k_values
            targets.extend([
                expand(f"results/{project}/dapc/{project}.dapc.K{{k}}.results.rds",
                       k=dapc_k_values),
                expand(f"results/{project}/dapc/{project}.dapc.K{{k}}.cluster_assignments.txt",
                       k=dapc_k_values),
                expand(f"results/{project}/dapc/{project}.dapc.K{{k}}.membership_probs.txt",
                       k=dapc_k_values),
                expand(f"results/{project}/dapc/plots/{project}.dapc.K{{k}}.scatter.pdf",
                       k=dapc_k_values),
                f"results/{project}/dapc/plots/{project}.dapc.criterion_plot.pdf"
            ])
            # Only add map plots if popdata is specified
            if _has_popdata(params):
                targets.extend([
                    expand(f"results/{project}/dapc/plots/{project}.dapc.K{{k}}.map.pdf",
                           k=dapc_k_values),
                    expand(f"results/{project}/dapc/plots/{project}.dapc.K{{k}}.map.rds",
                           k=dapc_k_values)
                ])
                if dapc_k_values:
                    targets.extend(_map_facet_targets(project, "dapc"))

    return targets

# Main target rule
rule all:
    input:
        get_all_targets()
